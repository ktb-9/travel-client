import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { position: "absolute", top: 0, width: "100%", borderWidth: 2 },
  background: {
    width: "100%",
    backgroundColor: "black",
    height: 300,
  },
});

export default styles;
